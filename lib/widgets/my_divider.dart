import 'package:flutter/material.dart';

class MyDividerMoreScreen extends StatelessWidget {
  const MyDividerMoreScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Colors.white,
      thickness: 0.3,
    );
  }
}
